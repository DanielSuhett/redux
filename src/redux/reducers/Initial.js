export const Types = {};

export const Creators = {};

const INITIAL_STATE = {};

export function Reducer(state = INITIAL_STATE, action) {
  switch (action.type) {
    default:
      return state;
  }
}
